[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --cov=agents
    --cov=core
    --cov=database
    --cov-report=html
    --cov-report=term-missing
    --cov-fail-under=60
asyncio_mode = auto
